package test.text;

public class Thermo {
//A A B C D E B A z
/*
    public String findSequencePalindrome(String input{
        char[] arr = str.toCharArray();
        for(int i =0; i<arr.length; i++){
            int j = i+1;

            String str= input.substring(i,j);

            List<> list = findPalindrome(arr, str, j)
        }




    }

    public String findPalindrome(char[] arr , String str, int j){
        Strng palindrome = null;
        char[] compareArr= str.toCharArray();
        String temp = str;

        for(int i =0; i<compareArr.length; i++){
            boolean found = false;
            for(;j<arr.length; ){
                if(compareArr[i] == arr[j]){
                    temp = temp+arr[j];
                    found = true;

                }
                if(!found){
                    break;
                }//2nd for loop
            } //1st for loop ends
            if(found){
                palindrome = temp;
            }
            return palindrome;


        }

 */
}
